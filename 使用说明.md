# HousePlusMail 使用说明

HousePlusMail 是一个自托管、多用户、多邮件账户的 Web 邮件客户端。本说明面向**最终使用者**：如何登录、添加邮箱、收发邮件、管理附件与多设备访问。不含部署（部署见《部署指南》）。

---

## 1. 登录与退出

- 打开前端地址（例如 `https://app.yourdomain.com`）。
- 输入**用户名**与**密码**登录。
- 右上角「退出」可注销当前会话。
- 会话使用 HttpOnly Cookie + CSRF 防护，有效期 7 天。

> **关于账号**：当前版本仅实现「本地账号（local）」。首次部署时用环境变量创建了**管理员账号**，用它登录即可。界面暂未提供自助注册 / 找回密码功能（数据模型支持多用户，但无“新建用户”页面）。对自用场景，一个管理员账号足够。

---

## 2. 添加邮件账户（最关键的一步）

「账户管理」→「+ 添加账户」，填写：

| 字段 | 说明 |
| --- | --- |
| 显示名称 | 便于区分，如「工作 Gmail」「个人 QQ」 |
| 邮箱地址 | 完整邮箱，如 `you@example.com` |
| 登录用户名 | 多数与邮箱相同；部分服务商要求特定用户名，按其要求填 |
| 密码 | 邮箱的**应用专用密码 / 授权码**，**不是**网页登录密码 |
| IMAP（收件） | 主机、端口、加密方式 |
| SMTP（发件） | 主机、端口、加密方式 |
| 代理（可选） | 直连 / HTTP / SOCKS5，默认直连 |
| 设为默认账户 | 勾选后作为登录后默认选中的账户 |

### 关于“密码”的重要提醒

很多邮箱**不能直接用网页登录密码**通过 IMAP/SMTP 收发，需要开启“应用专用密码 / 授权码”：

- **Gmail**：开启两步验证后，在 Google 账号 → 安全 → 应用专用密码，生成 16 位 App Password。
- **QQ 邮箱**：设置 → 账户 → 开启 IMAP/SMTP 服务，按提示发短信获取**授权码**。
- **163/126 邮箱**：设置 → POP3/SMTP/IMAP → 开启并获取授权码。
- **Outlook/Hotmail**：通常用微软账号密码（若开启 2FA 也需应用密码）。

### 常见服务商 IMAP / SMTP 参考

| 服务商 | IMAP 主机 : 端口 : 加密 | SMTP 主机 : 端口 : 加密 |
| --- | --- | --- |
| Gmail | `imap.gmail.com` : 993 : TLS | `smtp.gmail.com` : 465 : TLS |
| Outlook / Hotmail | `outlook.office365.com` : 993 : TLS | `smtp.office365.com` : 587 : STARTTLS |
| QQ 邮箱 | `imap.qq.com` : 993 : TLS | `smtp.qq.com` : 465 : TLS |
| 163 邮箱 | `imap.163.com` : 993 : TLS | `smtp.163.com` : 465 : TLS |
| 126 邮箱 | `imap.126.com` : 993 : TLS | `smtp.126.com` : 465 : TLS |

> 加密方式可选 `TLS（隐式，端口通常 993/465）` 或 `STARTTLS（端口通常 143/587）`，按服务商要求选择。

### 连接测试

保存前（或编辑时）点「**测试连接**」：

- IMAP ✅ 且 SMTP ✅ → 连通正常，可保存。
- 任一项 ❌ → 会显示错误原因（多为**主机/端口/加密方式/密码**不对，或邮箱未开启 IMAP 服务）。

### 编辑 / 删除

- 在账户列表点「编辑」：可改配置；**密码留空 = 不修改原密码**，填了就改。
- 点「删除」：移除该账户，同时清除本地存储的凭据。

---

## 3. 浏览邮件

- **切换账户**：顶部下拉框选择当前操作的邮箱账户。
- **文件夹**：左侧列出该账户的文件夹（INBOX、已发送、草稿、垃圾邮件等，按你邮箱实际结构）。点击切换。
- **邮件列表**（中间）：显示发件人 / 主题 / 预览 / 时间；**加粗 = 未读**。
  - 默认显示 50 封，可点「加载更多」继续加载，最多 200 封。
- **查看邮件**：点击任意邮件，右侧显示详情。
  - **打开即自动标记为已读**。
  - 正文：HTML 邮件以内联方式渲染；纯文本以等宽格式显示。
  - 附件：小附件可直接下载；**过大的附件仅显示大小（本版限制，不内联加载）**。

---

## 4. 回复 / 回复全部 / 转发

在邮件详情下方按钮：

- **回复**：收件人 = 原发件人；主题加 `Re:`；正文自动引用原文。
- **回复全部**：收件人 = 原发件人，抄送 = 原收件人（去掉自己）；主题 `Re:`。
- **转发**：主题加 `Fwd:`；正文引用原文；需自行填写收件人。

点击任一按钮会弹出撰写框并预填相关内容，可直接修改后发送。

---

## 5. 撰写与发送

入口：顶部「**写邮件**」，或邮件详情里的回复/转发。

- 收件人：（逗号 / 分号分隔可填多个）
- 抄送（CC）、密送（BCC）：可选
- 主题、正文
- **附件**：点「选择文件」可添加多个；已添加的附件点 × 移除。
- **发送**：从**当前选中的账户**发出（发件人 = 该账户邮箱）。
  - 发送成功后会**尽力**把邮件归档到「已发送」文件夹（候选名：`Sent` / `Sent Items` / `Sent Mail` / `已发送` / `INBOX.Sent`）。若你的邮箱“已发送”文件夹名不在候选里，则不会归档，但**邮件本身已成功发出**。
- 发送后列表自动刷新。

> 注意：回复/转发时，邮件仍从顶部当前选中的账户发出，而不是“原收件账户”。如需用特定邮箱发，请先在顶部切换账户。

---

## 6. 删除邮件

- 邮件详情「**删除**」→ 确认后从邮件服务器删除（IMAP 删除标记/清除）。
- 删除后列表刷新；此操作直接作用于邮箱服务器，不可在应用内撤销（可在邮箱网页端“已删除/回收站”恢复，取决于服务商）。

---

## 7. 多设备使用

- 只要后端公网可达，任意设备的浏览器访问前端即可使用，**邮件账户集中在后端**，无需每台设备单独配置。
- 会话 Cookie 跨同注册域子域有效；在不同设备登录同一账号，数据一致。
- 在任意设备「退出」只清除该设备的会话。

---

## 8. 安全与备份

- **密码加密存储**：邮件账户密码以 AES-256-GCM 加密后落盘（`vault.key` 或 `HPM_VAULT_SECRET` 派生密钥）。API 永不返回明文密码。
- **传输与会话**：会话 Cookie 为 HttpOnly，仅在 HTTPS 下标记 Secure；写操作需 CSRF Token。
- **后端备份**：定期备份后端数据目录（默认 `/app/data`，含 `store.json` 与 `vault.key`）。
  - 用随机 `vault.key`：务必连同 `vault.key` 一起备份，否则无法解密已存密码。
  - 用 `HPM_VAULT_SECRET`：备份时保持该值不变。
- **强密码**：部署时务必改掉 `HPM_BOOTSTRAP_ADMIN_PASSWORD` 的占位值。

---

## 9. 已知限制

- 仅本地账号体系，无自助注册 / 找回密码界面（管理员由环境变量创建）。
- 附件过大不内联，仅显示大小。
- **无搜索、无标签/规则、无日历/通讯录**（本版刻意精简，只做多邮件管理）。
- 后端必须长驻运行，不能跑在纯 Serverless 上。
- **会话存于后端内存**：后端重启会清空所有登录会话，用户需重新登录。
- 单账号多端同时登录均有效（无端级互踢）。

---

## 10. 快速上手清单

1. 用部署时设的管理员账号登录。
2. 「账户管理」→「+ 添加账户」，按第 2 节填好 IMAP/SMTP 与**授权码**。
3. 点「测试连接」，确认 IMAP/SMTP 都 ✅，保存。
4. 回到主界面，左侧选文件夹，点邮件阅读；用「回复/转发/写邮件」收发。
5. 其他设备直接用浏览器打开同一前端地址登录即可。
