# HousePlusMail 部署指南（一步步）

本指南面向「想在公网、多设备上用网页收发邮件」的自用场景。本项目采用 **Cloudflare Pages（前端）+ Render 免费档（后端）**，前后端同属一个注册域的两个子域，Cookie 同站、部署最省心。文末也给出单实例 Docker 备用方案与各备选后端。

> 仓库：`https://github.com/JACKHU0006/HousePlusMail`
> 前端技术栈：React + Vite；后端：Node.js + Express + TypeScript（tsx 运行）。

---

## 0. 部署前须知（必读）

- **架构**：有状态后端（需持久文件系统 + 长生命周期 IMAP/SMTP 连接）+ 静态前端（React/Vite）。
- **后端不能跑纯 Serverless**（Vercel Functions / Cloudflare Workers）：它需要持久化文件（`store.json`、`vault.key`）和长连接。必须跑在容器/主机上。
- **数据位置**：后端 `HPM_DATA_DIR`（Docker 默认 `/app/data`）下存：
  - `store.json`：用户与邮件账户（密码为 AES-256-GCM 加密密文）。
  - `vault.key`：加密密钥（或改用环境变量 `HPM_VAULT_SECRET` 派生密钥）。
  - **务必把该目录挂成持久卷/盘并定期备份**，否则重启丢数据或无法解密。
- **首次启动必须创建管理员**：通过环境变量 `HPM_BOOTSTRAP_ADMIN_USERNAME` / `HPM_BOOTSTRAP_ADMIN_PASSWORD`。若本地认证开启却没配这两项，进程会直接退出。
- **会话是内存态**：登录会话保存在后端内存中，后端重启会清空所有会话，用户需重新登录（这是已知限制，见使用说明）。
- **后端平台选型（本项目采用）**：**Render 免费档**（零成本、Docker 原生、自带持久盘；免费档会休眠，首次访问约 30 秒冷启动，自用足够）。若需常驻不休眠，改用 **Railway**（约 $5/月）。Cloudflare Containers 需升级 **Workers Paid 付费计划**，常驻会持续计费，故仅作备选。

---

## 1. 准备工作

你需要：

1. **GitHub 仓库**：代码已就绪（`JACKHU0006/HousePlusMail`）。你也可以 fork 成自己的仓库再部署。
2. **域名与 DNS**：本项目实际域名是 `ccwu.cc`（DNS 由 **dnshe.org** 管理，不是 Cloudflare）。Cloudflare 只接管了子域 `hpmail.ccwu.cc`（CNAME 接入），并未接管整个 `ccwu.cc`。
   - 前端：`hpmail.ccwu.cc`（已在 Cloudflare，用作 Pages 自定义域）
   - 后端：`api.ccwu.cc`（同属 `ccwu.cc` → 会话 Cookie 同站）
   - 后端自定义域的 CNAME 需要加在 **`ccwu.cc` 的 DNS（dnshe.org）**，不是 Cloudflare。
3. **Cloudflare 账号**（免费版即可，仅用于前端 Pages）。
4. **Render 账号**（免费档即可，用于后端）。

规划两个子域（同注册域）：

```
hpmail.ccwu.cc   → 前端（Cloudflare Pages）
api.ccwu.cc      → 后端（Render 免费档）
```

两者同属 `ccwu.cc` → 会话 Cookie 为 `sameSite=lax`，可跨子域正常发送，多设备登录无碍。

---

## 2. 部署前端（Cloudflare Pages）

1. 登录 Cloudflare 控制台 → 左侧 **Workers & Pages** → **Create** → **Pages** → **Connect to Git**。
2. 授权 GitHub 并选择仓库 `JACKHU0006/HousePlusMail`（或你的 fork）。
3. 构建设置：
   - **Project name**：`houseplusmail`（随意）
   - **Production branch**：`main`
   - **Root directory（根目录）**：`web`（让构建在 `web` 目录内进行）
   - **Build command**：`npm install && npx vite build --outDir dist`
   - **Output directory**：`dist`
   - **环境变量（Environment variables）**：
     - `VITE_API_BASE` = `https://api.ccwu.cc/api/v1` ⚠️ **必须含 `/api/v1`**
4. 点击 **Save and Deploy**，等待构建（首次约 1–2 分钟）。
5. 构建成功后，进入项目 **Settings → Custom domains**，添加 `hpmail.ccwu.cc`。Cloudflare 已接管该子域，会自动接管并签发 HTTPS 证书（无需再加 CNAME）。

---

## 3. 部署后端（Render 免费档，本项目采用）

> ⚠️ Render 免费档 **会休眠**：15 分钟无访问即停止，下次访问冷启动约 30 秒。对自用、访问不频繁足够；若要常驻不休眠请改用 Railway（§4.1）。

1. Render 控制台 → **New → Web Service** → 连接本仓库 `JACKHU0006/HousePlusMail`。
2. **Runtime** 选 **Docker**（使用仓库根目录 `Dockerfile`）。
3. **Environment** 添加：
   - `HPM_BIND` = `0.0.0.0:8080`
   - `HPM_BOOTSTRAP_ADMIN_USERNAME` = `admin`
   - `HPM_BOOTSTRAP_ADMIN_PASSWORD` = **强密码（务必改掉占位值）**
   - `HPM_CORS_ORIGIN` = `https://hpmail.ccwu.cc`
4. **Disk** 中挂一块持久盘到 `/app/data`（存 `store.json` 与 `vault.key`）。
5. 部署后生成 `*.onrender.com` 地址；进入 **Settings → Custom Domain** 添加 `api.ccwu.cc`，并在 **`ccwu.cc` 的 DNS（dnshe.org）** 里把 `api` 的 CNAME 指向 Render 提供的目标地址。
6. 验证：访问 `https://api.ccwu.cc/api/v1/health` 应返回 `{"status":"ok",...}`。

### 本项目的环境变量（ccwu.cc）

**前端 Pages（构建/运行时环境变量）**

| 变量 | 值 |
| --- | --- |
| `VITE_API_BASE` | `https://api.ccwu.cc/api/v1` ⚠️ **末尾一定要带 `/api/v1`** |

**后端 Render（环境变量）**

| 变量 | 值 |
| --- | --- |
| `HPM_BIND` | `0.0.0.0:8080` |
| `HPM_DATA_DIR` | `/app/data`（并把持久盘挂到这里） |
| `HPM_BOOTSTRAP_ADMIN_USERNAME` | `admin`（或你想要的名字） |
| `HPM_BOOTSTRAP_ADMIN_PASSWORD` | **一个你从没在其他地方用过的强密码**（必改占位值） |
| `HPM_CORS_ORIGIN` | `https://hpmail.ccwu.cc` |
| `HPM_VAULT_SECRET`（可选） | 一个足够长的随机字符串 |

仓库根目录有 **`server/.env.example`**，里面是同一份参考，可直接复制。

---

## 4. 备选后端

### 4.1 Railway（常驻、持久盘、Docker 原生，付费）

> 适合「常驻不休眠」场景（约 $5/月）。比 Cloudflare Containers 更成熟；若你接受付费换常驻，这是首选备选。

1. Railway 控制台 → **New Project** → **Deploy from GitHub repo** → 选择 `JACKHU0006/HousePlusMail`（或你的 fork）。
2. 部署方式选 **Deploy a Dockerfile**（仓库根目录已包含 `Dockerfile`）。
3. **Variables** 中添加（同 §3 的变量表，仅 `HPM_CORS_ORIGIN` 仍为 `https://hpmail.ccwu.cc`）。
4. **Volume** 中挂一块持久盘到 `/app/data`（Railway 会自动映射 `HPM_DATA_DIR=/app/data`）。
5. 部署完成后，Railway 会给出一个 `*.railway.app` 地址；进入 **Settings → Domains** 添加自定义域 `api.ccwu.cc`，并在 `ccwu.cc` 的 DNS（dnshe.org）里把 `api` 的 CNAME 指向 Railway 提供的目标地址。
6. 验证：访问 `https://api.ccwu.cc/api/v1/health` 应返回 `{"status":"ok",...}`。

### 4.2 Cloudflare Containers（需 Workers Paid 付费）

> ⚠️ Cloudflare Containers 需要 **Workers Paid 付费计划**，常驻会产生按量费用，且仍处 Beta、控制台路径/术语可能变化。除非你特别想要「前后端都在 Cloudflare」，否则不推荐。

1. Cloudflare 控制台 → **Containers** → **Create Container App**。
2. 连接同一 GitHub 仓库，使用根目录的 `Dockerfile`。
3. 配置：Instance name `hpm-backend`、Port `8080`、卷挂 `/app/data`、环境变量同上（`HPM_CORS_ORIGIN=https://hpmail.ccwu.cc`）。
4. Deploy 后加自定义域 `api.ccwu.cc`。

示意 `wrangler.toml`（以 Cloudflare 当前文档为准）：

```toml
name = "houseplusmail"
pages_build = { dir = "web/dist" }
[[containers]]
name = "hpm-backend"
image = { dockerfile = "Dockerfile" }
port = 8080
volume = { name = "hpm-data", mount = "/app/data" }
```

---

## 5. DNS 与 Cookie（同域关键）

- 前端 `hpmail.ccwu.cc` 已在 Cloudflare（CNAME 接入的子域），Pages 自定义域会自动接管。
- 后端 `api.ccwu.cc` 的 CNAME 需加在 **`ccwu.cc` 的 DNS（dnshe.org）**，不是 Cloudflare。
- 两个子域同属 `ccwu.cc`，`sameSite=lax` 的会话 Cookie 即可在前后端间正常发送。
- 后端默认在反向代理提供 HTTPS（`x-forwarded-proto: https`）时把 Cookie 标记为 `Secure`，所以两端都要是 HTTPS。

---

## 6. 验证部署

1. 打开 `https://hpmail.ccwu.cc` → 应看到 HousePlusMail 登录页。
2. 用管理员账号登录。
3. 进入「账户管理」→「+ 添加账户」，填好邮箱后点「测试连接」，IMAP 与 SMTP 应都显示 ✅。
4. 后端健康检查：浏览器/命令行访问 `https://api.ccwu.cc/api/v1/health`
   应返回：`{"status":"ok","name":"houseplusmail","version":"0.1.0"}`。

---

## 7. 备用方案：单实例 Docker（前后端同进程）

不想拆分前后端时，最简单——一个容器同时构建前端并由后端托管 `server/public`：

```bash
# 在仓库根目录
docker build -t houseplusmail .
docker run -d --name hpm -p 8080:8080 \
  -v $(pwd)/data:/app/data \
  -e HPM_BOOTSTRAP_ADMIN_USERNAME=admin \
  -e HPM_BOOTSTRAP_ADMIN_PASSWORD='改成强密码' \
  houseplusmail
```

然后前置 Nginx/Caddy 反代 8080 并配置 HTTPS，用你自己的域名直接访问。此模式下前端 API 走相对路径 `/api/v1`，无需 `VITE_API_BASE` 与 `HPM_CORS_ORIGIN`。

---

## 8. 环境变量速查表

| 变量 | 用于 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `VITE_API_BASE` | 前端（构建时） | 空（用 `/api/v1`） | API 基址，**需含 `/api/v1`**；拆分部署填 `https://api.ccwu.cc/api/v1` |
| `HPM_BIND` | 后端 | `0.0.0.0:8080` | 监听地址:端口 |
| `HPM_DATA_DIR` | 后端 | `data` | 数据目录（Docker 已设 `/app/data`） |
| `HPM_AUTH_MODE` | 后端 | `local` | 本版仅实现 `local` |
| `HPM_BOOTSTRAP_ADMIN_USERNAME` | 后端 | 无 | 首次启动创建的管理员用户名（必填） |
| `HPM_BOOTSTRAP_ADMIN_PASSWORD` | 后端 | 无 | 管理员密码（必填，务必强密码） |
| `HPM_CORS_ORIGIN` | 后端 | 无 | 拆分部署时允许的前端来源，逗号分隔或 `*` |
| `HPM_VAULT_SECRET` | 后端 | 无 | 固定保险库密钥（可选；备份时需与数据一起保管） |
| `HPM_INSECURE_TLS` | 后端 | 无 | 私有 CA / 自签名证书场景跳过 TLS 校验（可选） |

---

## 9. 常见问题 / 排错

- **登录页打不开 / 白屏**：检查 Pages 构建是否成功；确认 `VITE_API_BASE` 填的是完整后端地址且**包含 `/api/v1`**。
- **登录后报错 / 跨域（CORS）**：检查后端 `HPM_CORS_ORIGIN` 是否等于前端来源（含 `https://` 与域名，无尾斜杠）；确认前后端是同一注册域的两个子域。
- **总是跳回登录（Cookie 不生效）**：确认前后端同注册域子域；确认两端都是 HTTPS（Cloudflare 默认提供，使 Cookie 标记 `Secure`）。
- **重启后账户消失 / 密码无法解密**：数据卷 `/app/data` 没持久化，或换了 `vault.key` 而 `HPM_VAULT_SECRET` 不一致。
- **后端起不来并报“未配置启动管理员”**：本地认证开启但缺少 `HPM_BOOTSTRAP_ADMIN_USERNAME/PASSWORD`，补上再启动。
- **Render 免费档首次访问很慢 / 提示 unavailable**：这是免费档休眠后的冷启动（约 30 秒），等一会再刷新即可，属正常。
- **端口相关**：后端必须监听 `8080`。

---

## 10. 备份与升级

- **备份**：定期备份后端 `HPM_DATA_DIR`（默认 `/app/data`）。若用随机 `vault.key`，务必连同 `vault.key` 一起备份；若用 `HPM_VAULT_SECRET`，备份时保持该值不变。
- **升级**：拉取最新代码 → 重新部署（Render/Railway 会自动重新构建）→ 数据在持久卷里，不受影响。

---

## 11. 本项目实战清单（ccwu.cc）

针对本项目的真实域名，端到端步骤汇总：

**前端（Cloudflare Pages）**
1. Pages 项目 → **Settings → Custom domains** → 添加 `hpmail.ccwu.cc`（Cloudflare 已接管该子域，会自动接管，无需再加 CNAME）。
2. 构建环境变量：`VITE_API_BASE=https://api.ccwu.cc/api/v1`（务必带 `/api/v1`）。
3. 确认构建成功并部署。

**后端（Render 免费档，本项目采用）**
1. **New → Web Service → 连接 `JACKHU0006/HousePlusMail` → Runtime 选 Docker**。
2. **Environment**：`HPM_BIND=0.0.0.0:8080`、`HPM_BOOTSTRAP_ADMIN_USERNAME=admin`、`HPM_BOOTSTRAP_ADMIN_PASSWORD=<强密码>`、`HPM_CORS_ORIGIN=https://hpmail.ccwu.cc`。
3. **Disk** 挂 `/app/data` 持久盘。
4. **Settings → Custom Domain** → 添加 `api.ccwu.cc`；Render 给出目标后，去 **`ccwu.cc` 的 DNS（dnshe.org）** 加 CNAME：`api` → Render 目标。
5. 注意：免费档会休眠，首次访问约 30 秒冷启动，属正常。

**验证**
- `https://hpmail.ccwu.cc` 登录页可打开。
- `https://api.ccwu.cc/api/v1/health` 返回 `{"status":"ok",...}`。
- 登录后「账户管理」加邮箱，点「测试连接」IMAP/SMTP 均 ✅。
